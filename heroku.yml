setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: production
    NODE_ENV: production
release:
  image: web
  command:
    - bin/vite build
run:
  web: ./bin/bundle exec puma -C config/puma.rb
